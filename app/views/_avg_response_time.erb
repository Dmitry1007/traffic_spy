<h3>Average response times:</h3>
<ul>
  <% @dataset.breakdown_of_avg_url_response_times.each do |url, time| %>
    <li><a href="<%= @dataset.url_path(url) %>" class="url-link"><%= url %></a> had an average response time of <%= time.to_i %> seconds</li>
  <% end %>
</ul>
